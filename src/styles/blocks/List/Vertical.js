import styled from 'styled-components';
import List from '../../../containers/List';

const Vertical = styled(List)`
  width: 100%;
  list-style-type: none;
  padding: 0;
`;

export default Vertical;
